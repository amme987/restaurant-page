const contact = () => {
  const content = document.getElementById("content");

  const test = document.createElement("div");
  content.appendChild(test).textContent = "contact";
};

export default contact;
